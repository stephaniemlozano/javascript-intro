let orderIndex = 0
let currentWeather = 'Sunny'
let stateResident = 'Georgia'

const renderString1 = `${currentWeather} ${stateResident}`

let orderIndexPlus1 = orderIndex++

const daysInRow = 3
const myExpression = 'omg' 
const renderMyExpression = myExpression.toUpperCase()
const renderString2 = `LOL! ${currentWeather} for ${daysInRow} days!!`

console.log(renderString1)
console.log(renderString2)
